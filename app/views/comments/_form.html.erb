<%= form_with model: comment do |form| %>
    <p>
      <%= form.label :body %><br>
      <%= form.text_area :body %>
    </p>
    <p>
      <%= form.submit %>
    </p>
    <%= form.hidden_field :profile_id %>
  <% end %>
  