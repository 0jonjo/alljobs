<h4><%= I18n.t('change_status') %></h4>

<%= button_to I18n.t('published'), published_job_path(@job.id) unless @job.published? %>
<%= button_to I18n.t('draft'), drafted_job_path(@job.id) unless @job.draft? %>
<%= button_to I18n.t('archived'), archived_job_path(@job.id) unless @job.archived? %> 


