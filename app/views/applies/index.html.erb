<h3><%= I18n.t('applies') %></h3>

<% if @applies.any? %>
  <% @applies.each do |apply| %>
     <%= render apply %>
  <% end %>  
<% else %>  
  <p>There aren't any apply.</p>
<% end %>

<%= paginate @applies %>

