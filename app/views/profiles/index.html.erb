<h3>Profiles</h3>

<% if headhunter_signed_in? %>
  <% @profiles.each do |profile| %>
    <p>
    <%= link_to profile.name, profile %><br>
    Social Name: <%= profile.social_name %><br>
    User ID: <%= profile.user_id %><br>
    </p>
  <% end %>
<% end %>

<%= paginate @profiles %>

<%= link_to "Return to Home", root_path %>